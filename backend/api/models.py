# Models are intentionally minimal.
# All data (users, leaves) is stored directly in MongoDB Atlas via pymongo.
# Django's ORM is NOT used for application data.
# This file exists only to satisfy Django's app structure expectations.
